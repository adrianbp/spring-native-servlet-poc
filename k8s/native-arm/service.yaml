apiVersion: v1
kind: Service
metadata:
  name: spring-native-app-arm-service
  namespace: spring-native-arm
  labels:
    app: spring-native-app-arm
    version: native
    architecture: arm64
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: spring-native-app-arm
    version: native
